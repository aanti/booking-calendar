import * as Dates from './dates/dates'
import classes from './classes/classes'
import { outsideClickable } from './hoc'

const nArray = n => Array.from(Array(n).keys())

export { Dates, classes, nArray, outsideClickable }
